#include "main.h"

/**
 * _abs - function that give the absolute valute of number
 * @nb: Nb input
 * Return: Absolute value of number
 */
int _abs(int nb)
{
	if (nb >= 0)
	{
		return (nb);
	}
	else
	{
		return (nb);
	}
}
