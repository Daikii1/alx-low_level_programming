#include "main.h"
/**
 * _abs - its a function to Verify absolute of number
 * @NB: input
 * Return: return -NB if NB<0 and return NB if its not
*/
int _abs(int NB)
{
	if (NB < 0)
	{
		return (-NB);
	}
	else
	{
		return (NB);
	}
}
